# Production values are hardcoded as fallbacks in src/lib/supabase.js.
# Do NOT define VITE_SUPABASE_ANON_KEY here — esbuild wraps long inlined
# strings with newlines inside template literals, which breaks the Headers API.
