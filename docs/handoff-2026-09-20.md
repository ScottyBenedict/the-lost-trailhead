# Handoff — 2026-09-20

Covers the evening of 2026-09-19 and the morning of the 20th. Tools are in
`scripts/flyover-check/` (see its README).

**Shipped in that stretch:** PR #40 photo weight and the Supabase outage,
#41 the range map, #42 Blanca Lake, #43 mobile, #44 the hiker marker
(reverted by #46), #45 admin duplicate detection.

**How pushes work here:** never push without Scott's go-ahead. He reviews
the change in code by default and calls for the dev server himself when he
thinks something warrants it — don't insist on a localhost pass for every
change, and don't skip the go-ahead for any.

## 0. Getting started (do this first, every session)

1. Repo: `~/Desktop/the-lost-trailhead` (Scott's local clone; this is where
   Claude Code edits). `git switch main && git pull --ff-only`; run
   `npm install` if `package.json` changed.
2. **Start the local dev server** and leave it running in the background:
   `npm run dev` (serves http://localhost:5173 — Vite hot-reloads edits). Confirm
   it answers (`curl -s -o /dev/null -w '%{http_code}' http://localhost:5173/`
   → 200) and tell Scott it's up. Scott reviews every change on localhost before
   anything is pushed.
   - For phone testing use `npm run dev -- --host` instead; Scott's phone opens
     `http://<iMac Wi-Fi IP>:5173` (`ipconfig getifaddr en1`, was 192.168.0.85).
     The macOS firewall already allows node. If anything will trigger a macOS
     permission/firewall prompt, **tell Scott before running it**.
3. Check scripts: `cd scripts/flyover-check && npm install` (once), per its
   README.
4. Work on a branch; when Scott says push: commit, open a PR, merge, then verify
   the live site (https://the-lost-trailhead.vercel.app) picked up the change.

## 1. Range map on the About page — SHIPPED (PR #41), desktop only

Live on `/about`. Vector water (`src/data/water.json`), state lines drawn
over the map as polylines, flat 2D Mercator so the 49th parallel is level
and eastern Washington is vertical. Rebuild the data with
`scripts/flyover-check/fetchwater.py` then `fetchborders.py`, and check it
with `watercheck.py` (30 landmarks, offline, no browser).

**It is switched off below 700px.** Cesium is not even fetched there — see
`MIN_MAP_WIDTH` in `src/components/RangeMap.jsx`. Scrolling the About page
on an iPhone exhausted the tab and Safari reloaded it; capping the relief
tiles and handling `webglcontextlost` were not enough.

**The fix, when someone picks this up:** the map is a fixed, north-up,
non-interactive picture, so it does not need a 3D engine on any device.
Generate the relief once as a committed image and place the pins as
absolutely-positioned HTML by a Mercator transform. That works on phones,
drops a 663 kB chunk, makes the pins real tappable elements, and renders
identically every load. Everything needed to produce the image already
exists in `scripts/flyover-check/`.

Known cosmetic issue: a dozen hikes cluster around Snoqualmie Pass and
their pins overlap. Pins scale down below 700px (`PIN_PX` in
`src/lib/rangeMap.js`) but that only helps so much.

## 2. Snow-free flyover imagery (approved, paused)

**Scott's rule:** every flyover should use snow-free imagery at the highest
available resolution. If the only snow-free option is lower resolution (or
there isn't one), **discuss with Scott before switching**.

**Snow in the current imagery** (`snowcheck.py`): Hidden Lake 17.9% avg / 37.7%
worst tile, Cascade Pass 10.9 / 29.3, Lake Valhalla 7.5 / 13.0, Lake Ingalls
3.6 / 32.3, Manastash 2.9 / 18.9. All other hikes ~0.

**Option:** Esri's Wayback archive (196 releases since 2014; CORS-enabled). The
current imagery for Valhalla is a 2025-03-01 WorldView-3 capture at **0.31 m**
(snowy). The newest snow-free one is Wayback release **48624** (2021-11-30),
which serves a 2015-07-02 WorldView-2 capture at **0.5 m** — lower source
resolution, so this is a discuss-first case. At the flyover's 900 m camera range
each screen pixel covers ~1–2 m of ground, so the difference may not be visible.

**Next steps:**
1. Add an optional per-hike Wayback release to the flyover: a map like
   `SATELLITE_RELEASE = { 'lake-valhalla': 48624 }` in `HikeMap.jsx`, passed to
   `TerrainFlyover` as `imageryRelease`; when set, the satellite layer's URL
   becomes the Wayback template in `scripts/flyover-check/common.py`
   (`WAYBACK_TILE`), otherwise unchanged. Other hikes must not change.
2. Let Scott compare Valhalla on localhost with and without it (his call on
   fidelity).
3. Run `wbsearch.py` for Hidden Lake, Cascade Pass, Lake Ingalls and Manastash
   and bring Scott the options (snow %, source date, resolution vs current).

## Also open

- **Trailing camera** is on 18 of 33 hikes. Still to add: Annette, Snow Lake
  (+ Winter), Subway Cave, Mt. Si — Winter, Mailbox Old Trail, Silver Peak,
  Lake Twenty-Two, Little Si, Camelback, Sourdough Ridge, Manastash.
- **Four hikes disagree with their track** and need a decision: Bandera
  (page 7.8 vs track 4.7), Manastash (3.7 vs 5.9), Oyster Dome (4.3 vs 6.8),
  Red Top (1.5 vs 0.8). **Run the grade profile over the first couple of
  miles before assuming the page is wrong.** Blanca looked like a 2.8 mi
  error and was a washed-out road: the first 1.5 mi gained 160 ft at under
  4%, then the grade stepped to 20% at the real trailhead. `TRAIL_START` in
  `src/lib/gpxFlyover.js` trims a track to the trailhead; the page distance
  was right all along. Manastash and Oyster Dome are the same shape
  (track longer than page). Bandera and Red Top run the other way, which is
  more likely a partial trip — see Cascade Pass, PR #17.
- **Any hike whose recording starts away from the trailhead has a wrong
  `trailhead` pin**, since those coordinates were taken from each GPX's
  first point. Blanca's pointed at the washout, 1.6 mi down the road.
- **GPX is now the heaviest thing on a hike page** at ~3 MB, more than all
  the photos together. `photopipe.py gpx` drops the trackpoint children the
  site never reads (course, hAcc, vAcc) for about a third, verified lossless
  for every field `gpxFlyover.js` parses. Moving the 31 files to `public/`
  would take them off Supabase entirely, where Vercel serves them free.
- **Hiker marker vs green dot — FIXED 2026-09-25 (branch `fix-end-dot-height`).**
  The diagnosis above this entry used to say the end dots were
  double-counted. That was wrong: the dots were never `pinPoint`; they
  are billboards at the line's own absolute height (terrain + 4 m) and
  were correct. The hiker was the problem. Cesium's RELATIVE_TO_GROUND
  clamp used a coarser ground than the line's terrain sampling; at
  Blanca's trailhead it was 14 m low, so the hiker drew ~10 px below the
  dot. Valhalla's gentler slope hid it (<1 m). Now the trail exposes its
  sampled heights (`trail.ground` in `trailPolyline.js`) and
  `placeMarker` in `terrainFlyover.js` stands the hiker on them. Until
  they arrive it falls back to the old clamp. The fix is shared code, so
  it covers every hike. **Verify from rendered pixels, not computed
  positions.** A probe that computed where the hiker *should* be said
  "aligned" while the render showed it 10 px off.
- Deferred bug: jumping the paused slider from 0% to ~4–20% can put the
  camera underground (playback is fine).
- Hikes done with no page yet: Smith Rock, Ptarmigan Ridge, Sauk Mountain,
  Teneriffe Falls.

## Admin duplicate detection (PR #45)

"Log a Trip" listed only the `hike_photos` rows, never the photos committed
into `hikes.js`. Bandera has six of those and had zero rows, so the tab said
"no photos" while the page showed six, and two got uploaded again (since
deleted, rows and objects). The strip now lists both and says which is
which, and `perceptualHash` in `adminUtils.js` catches a re-upload by sight
— SHA-256 cannot, because the same photograph exists as a repo WebP, an
older JPEG upload and a fresh WebP one. Measured on those Bandera photos:
real duplicates 9 bits, different shots of the same hike 24–28, threshold
14. It hashes thumbnails, not full photos — hashing full ones cost 18 MB
per hike selection.

Still to do: store the hash in a column so it is not recomputed every
selection and so duplicates across different hikes are caught too. That
pairs with **PR #1** (still open, deliberately), which captures
DateTimeOriginal + SubSecTimeOriginal + GPS on upload — the fingerprint
Scott's photo-library work identified as the only reliable identity key.
Its columns are already live and empty. Note the 133 pre-existing photos
have no recoverable capture metadata anywhere: the admin upload has always
re-encoded through a canvas, which strips EXIF, so it was gone before it
reached Storage.

## Photo weight and the Supabase outage (2026-09-19, PR #40)

Supabase restricted the project for exceeding the free plan's 5 GB monthly
CDN allowance and the live site lost bios, photos and trip data. Nothing had
ever been resized: 133 photos averaging 4 MB, a 2.5 MB avatar in a 180px
circle, ~28 MB per hike page. **Most of the traffic was ours** — localhost
pulls from the same CDN, and every `scan.mjs` / `card.mjs` / `range.mjs` run
loads a real hike page.

Now: photos resize in the browser on upload (WebP, 2880px, plus an 800px
`thumb_<name>` beside each one, found by convention with no DB column), the
buckets enforce 3 MB / 1 MB / 8 MB server-side whatever the client, and the
browser checks cache to `.cache/http`. `public/photos` went 220.8 MB → 52 MB;
Storage went 540 MB → 190 MB. A pre-migration backup of all 165 objects is at
`~/Desktop/tlt-storage-backup`, byte-verified — safe to delete once you are
happy.

The project is on Pro ($25/mo) to restore service. Egress is roughly 30x
lower now, so Free should be comfortable again — watch one billing cycle
before dropping back.
